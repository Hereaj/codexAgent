
# Database configuration (automatically provided by Replit PostgreSQL)
DATABASE_URL=your_postgresql_database_url_from_replit

# Admin credentials - CHANGE THESE IN PRODUCTION!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Server configuration
NODE_ENV=production
PORT=5000

# Security Notes:
# 1. Change ADMIN_PASSWORD to a strong password
# 2. The DATABASE_URL will be automatically set when you create a PostgreSQL database in Replit
# 3. All environment variables should be set in the Replit Secrets panel
